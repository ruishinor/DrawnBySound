{
  "name": "vibratoflow",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "VibratoFlow — local-first, real-time music-interpretation oscilloscope visualizer.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && node scripts/verify-dist.mjs && node scripts/verify-security-config.mjs",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:e2e:prod": "npm run build && playwright test -c playwright.prod.config.ts",
    "audit:production": "npm audit --omit=dev --audit-level=high",
    "audit": "npm audit --audit-level=high"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^22.10.2",
    "typescript": "^5.6.3",
    "vite": "^8.1.4",
    "vitest": "^4.1.10"
  }
}
